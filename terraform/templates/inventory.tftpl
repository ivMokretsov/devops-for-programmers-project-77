all:
  children:
    webservers:
      vars:
        ansible_user: yc-user
        ansible_ssh_private_key_file: ~/.ssh/id_rsa

      hosts:
        server1:
          ansible_host: "${vm1_ip}"

        server2:
          ansible_host: "${vm2_ip}"
